{
  "name": "bingo-app",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "build": "npm install --prefix client && npm run build --prefix client"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "fs-extra": "^11.3.0",
    "path": "^0.12.7"
  }
}
